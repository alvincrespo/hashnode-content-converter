#!/usr/bin/env node
/* eslint-disable no-console */

async function main() {
  console.log('CLI not yet implemented');
  process.exit(1);
}

main().catch(err => {
  console.error('Fatal error:', err);
  process.exit(1);
});
